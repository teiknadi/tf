const styles = {
  content: ({ index }) => ({
    display: index ? 'none' : 'block',
    position: 'absolute',
    width: '100%',
    height: '100%',
    verticalAlign: 'middle',
    fontSize: '.6875em'
  })
};

export default styles;
