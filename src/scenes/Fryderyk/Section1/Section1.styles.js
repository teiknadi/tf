import { defaultColumnWidth } from '../../../styles/constants';

const styles = {
  columns: {
    marginLeft: defaultColumnWidth
  },
  image: {
    width: 'auto',
    maxWidth: '100%',
    margin: '0 auto',
    display: 'block',
  },
};

export default styles;
