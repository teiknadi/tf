import { defaultColumnWidth } from '../../../styles/constants';

const styles = {
  wrapper: {
    padding: '100px 0',
    paddingLeft: defaultColumnWidth,
    position: 'relative',
  },
  image: {
    position: 'absolute',
    left: defaultColumnWidth,
    width: '100%',
  },
};

export default styles;
